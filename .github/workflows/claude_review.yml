# Copyright (c) 2026, NVIDIA CORPORATION. All rights reserved.
#
# Preserve Megatron-LM review behavior while separating read-only analysis from
# validated publication. This atomically replaces the former combined jobs.
# Rollback disables this workflow; it must not restore the combined path.
#
# multi-approval-bot consumes formal CODEOWNER approvals. The final LGTM below
# is deliberately a COMMENT and cannot satisfy that approval-state consumer.

name: Claude Code Review

on:
  issue_comment:
    types: [created]

permissions: {}

jobs:
  capture:
    name: Authorize command and capture revisions
    if: github.event.issue.pull_request
    runs-on: ubuntu-latest
    permissions:
      contents: read
      issues: write
      pull-requests: read
    outputs:
      active: ${{ steps.prepare.outputs.authorized == 'true' && steps.prepare.outputs.revision_match == 'true' }}
      artifact_name: claude-review-context-${{ github.run_id }}-${{ github.run_attempt }}
      base_sha: ${{ steps.prepare.outputs.base_sha }}
      head_sha: ${{ steps.prepare.outputs.head_sha }}
      merge_base_sha: ${{ steps.context.outputs.merge_base_sha }}
      mode: ${{ steps.prepare.outputs.mode }}
      pr_number: ${{ steps.prepare.outputs.pr_number }}
    steps:
      - name: Checkout trusted workflow revision
        uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6
        with:
          fetch-depth: 0
          persist-credentials: false

      - name: Parse, authorize, capture, react, and acknowledge
        id: prepare
        env:
          GITHUB_TOKEN: ${{ github.token }}
          CLAUDE_REVIEW_BOT_ALLOWLIST: ${{ vars.CLAUDE_REVIEW_BOT_ALLOWLIST }}
        run: |
          set -euo pipefail
          python .github/scripts/claude_review.py prepare-event \
            --event "$GITHUB_EVENT_PATH" \
            --output "$RUNNER_TEMP/claude-review-request.json" \
            --acknowledge

      - name: Fetch the captured PR head without checkout
        if: steps.prepare.outputs.authorized == 'true' && steps.prepare.outputs.revision_match == 'true'
        env:
          HEAD_SHA: ${{ steps.prepare.outputs.head_sha }}
          PR_NUMBER: ${{ steps.prepare.outputs.pr_number }}
        run: |
          set -euo pipefail
          git -c protocol.version=2 fetch --no-tags origin \
            "+refs/pull/$PR_NUMBER/head:refs/remotes/claude-review/head"
          test "$(git rev-parse refs/remotes/claude-review/head^{commit})" = "$HEAD_SHA"

      - name: Build inert immutable context
        id: context
        if: steps.prepare.outputs.authorized == 'true' && steps.prepare.outputs.revision_match == 'true'
        run: |
          set -euo pipefail
          python .github/scripts/claude_review.py build-context \
            --repo "$GITHUB_WORKSPACE" \
            --metadata "$RUNNER_TEMP/claude-review-request.json" \
            --output-dir "$RUNNER_TEMP/claude-review-context"
          python - "$RUNNER_TEMP/claude-review-context/metadata.json" <<'PY'
          import json
          import os
          import sys

          with open(sys.argv[1], encoding="utf-8") as stream:
              context = json.load(stream)
          with open(os.environ["GITHUB_OUTPUT"], "a", encoding="utf-8") as output:
              output.write(f"merge_base_sha={context['merge_base_sha']}\n")
          PY

      - name: Upload immutable context
        if: steps.prepare.outputs.authorized == 'true' && steps.prepare.outputs.revision_match == 'true'
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4.6.2
        with:
          name: claude-review-context-${{ github.run_id }}-${{ github.run_attempt }}
          path: ${{ runner.temp }}/claude-review-context
          if-no-files-found: error
          retention-days: 1

  review:
    name: Isolated ${{ needs.capture.outputs.mode }} review
    needs: capture
    if: needs.capture.outputs.active == 'true'
    uses: ./.github/workflows/_claude-review-isolated.yml
    permissions:
      contents: read
      id-token: write
    concurrency:
      group: claude-review-${{ github.repository }}-${{ github.event.issue.number }}-${{ needs.capture.outputs.mode }}
      cancel-in-progress: true
    with:
      artifact_name: ${{ needs.capture.outputs.artifact_name }}
      base_sha: ${{ needs.capture.outputs.base_sha }}
      head_sha: ${{ needs.capture.outputs.head_sha }}
      merge_base_sha: ${{ needs.capture.outputs.merge_base_sha }}
      mode: ${{ needs.capture.outputs.mode }}
      pr_number: ${{ fromJSON(needs.capture.outputs.pr_number) }}
    secrets:
      nvidia_inference_key: ${{ secrets.NVIDIA_INFERENCE_KEY }}
      nvidia_inference_url: ${{ secrets.NVIDIA_INFERENCE_URL }}
