.model_config: !include ../bert_mcore.yaml

ENV_VARS: !reference [.model_config, ENV_VARS]

MODEL_ARGS:
  <<: !reference [.model_config, MODEL_ARGS]
  --tensor-model-parallel-size: 2
  --pipeline-model-parallel-size: 2
  --spec: local
  --use-checkpoint-args: true
  --use-checkpoint-opt_param-scheduler: true
  --attention-backend: local

TEST_TYPE: !reference [.model_config, TEST_TYPE]
