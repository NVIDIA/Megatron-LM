ENV_VARS:
  # NOTE: CUDA_DEVICE_MAX_CONNECTIONS should be set to a larger value for A2A overlap
  CUDA_DEVICE_MAX_CONNECTIONS: 32
  NVTE_ALLOW_NONDETERMINISTIC_ALGO: 0
  PYTORCH_CUDA_ALLOC_CONF: expandable_segments:True
  NCCL_NVLS_ENABLE: 0
  PYTHONWARNINGS: ignore
  NCCL_DEBUG: VERSION

MODEL_ARGS:
  # Transformer Engine args
  --transformer-impl: transformer_engine
  # Distributed args
  --distributed-timeout-minutes: 60
  --tensor-model-parallel-size: 2
  --pipeline-model-parallel-size: 2
  --num-virtual-stages-per-pipeline-rank: 4
  --expert-model-parallel-size: 4
  --context-parallel-size: 1
  --expert-tensor-parallel-size: 1
  --use-distributed-optimizer: true
  --overlap-grad-reduce: true
  --overlap-param-gather: true
  --use-mcore-models: true
  --sequence-parallel: true
  --micro-batch-size: 4
  # MoE training related args
  --moe-grouped-gemm: true
  --moe-token-dispatcher-type: alltoall
  --moe-permute-fusion: true
  --save-interval: 25
  # A2A communication overlap args
  --overlap-moe-expert-parallel-comm: true
  --delay-wgrad-compute: true
  # Add mixed precision args
  --bf16: true
  --exit-interval: 50
  # kernel fusion related args
  --no-rope-fusion: true
  --cross-entropy-loss-fusion: true
  --cross-entropy-fusion-impl: native
  # MISC
  --manual-gc: true
  --manual-gc-interval: 100
TEST_TYPE: resume-ckpt

